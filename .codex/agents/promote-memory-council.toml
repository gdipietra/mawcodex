name = "promote_memory_council"
description = "Five-critic council that votes YES/NO on whether candidate `[LEARN]` entries should be promoted from `.codex/state/personal-memory.md` (gitignored) to MEMORY.md (committed). Each critic reviews one dimension—generality, staleness, redundancy, evidence, or format—in a fresh bounded task context. Invoked by `$promote-memory`."
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = "Resolve the repository root, then read and follow `references/agent-roles/promote-memory-council.md` completely. Stay within that narrow role, cite concrete evidence, distinguish PASS, FAIL, and UNVERIFIED, and return findings to the parent. Do not expand scope or perform external actions."
